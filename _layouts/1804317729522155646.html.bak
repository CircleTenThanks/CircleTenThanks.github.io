<!DOCTYPE html>
<html lang="{{ page.lang | default: site.lang | default: "ja_JP" }}">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link rel="stylesheet" href="{{ "/assets/css/style.css?v=" | append: site.github.build_revision | relative_url }}">
  </head>
  <body>
    <div class="container-lg px-3 my-5 markdown-body" style="overflow-x: auto;">
      {{ content }}
      <table style="width: 100%; table-layout: fixed;">
        <thead>
          <tr>
            <th scope="col">str</th>
            <th scope="col">gen</th>
            <th scope="col">member</th>
            <th scope="col">description</th>
          </tr>
        </thead>
        <tbody>
        {% for row in site.data.1804317729522155646 %}
          <tr>
            <td><span style="background: linear-gradient(transparent 70%, {{ row.color }} 0%);">{{ row.str }}</span></td>
            <td>{{ row.gen }}</td>
            <td>{{ row.member }}</td>
            <td>{{ row.description }}</td>
          </tr>
        {% endfor %}
        </tbody>
      </table>
    </div>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/anchor-js/4.1.0/anchor.min.js" integrity="sha256-lZaRhKri35AyJSypXXs4o6OPFTbTmUoltBbDCbdzegg=" crossorigin="anonymous"></script>
    <script>anchors.add();</script>
  </body>
</html>
